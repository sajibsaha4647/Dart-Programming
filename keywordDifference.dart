class Keyword {
  differencefinlandcost() {
    final age = 10; //this one is compile in run time
    const ages = 30; //this was in execute in compile time

    var name = "sajib";
    dynamic names = 32;

    // name = 43; // var takes one time variable type when it defined then it fixed
    names = "dajib"; //dynamic can take multiple time multiple value
    print("object");
  }
}
